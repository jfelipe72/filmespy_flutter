import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:bloc_pattern/bloc_pattern_test.dart';

import 'package:filmespy_flutter/app/screens/movies_favorites/movies_favorites_page.dart';

void main() {
  testWidgets('MoviesFavoritesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MoviesFavoritesPage(title: 'MoviesFavorites')));
    //  final titleFinder = find.text('MoviesFavorites');
    //  expect(titleFinder, findsOneWidget);
  });
}
